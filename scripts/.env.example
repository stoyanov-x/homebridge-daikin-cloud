# Remote deployment configuration
# Copy this file to .env and fill in your values.
# .env is gitignored, so your personal settings stay local.

REMOTE_HOST=my-server.local
REMOTE_USER=my-user
REMOTE_PLUGIN_DIR=/path/to/homebridge/custom
